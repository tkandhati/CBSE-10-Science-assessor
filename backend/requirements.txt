fastapi==0.111.0
python-dotenv>=1.0.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
aiosqlite==0.20.0
pydantic==2.7.1

# AI providers — only the key for the provider you use needs to be installed,
# but all three are listed so the environment is ready for any choice.
anthropic==0.26.0
google-generativeai>=0.8.0
openai>=1.30.0

pypdf==4.2.0
python-docx==1.1.2
pdfplumber==0.11.0
